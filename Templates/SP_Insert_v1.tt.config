# Restriction checkboxes for SP_Insert_v1.tt (see Docs/specs.md section 5.3).
# Insert doesn't require a primary key -- a table can be inserted into without one.
RequiresPrimaryKey=false
TableOnly=true
