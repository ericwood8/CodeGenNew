# Restriction checkboxes for SP_Save_v1.tt (see Docs/specs.md section 5.3).
# Save decides insert-vs-update by looking the row up by primary key, so a primary key is required.
RequiresPrimaryKey=true
TableOnly=true
