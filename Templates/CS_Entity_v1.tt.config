# Restriction checkboxes for CS_Entity_v1.tt (see Docs/specs.md section 5.3).
# The class needs a key ([Key]); the template further insists on a single-column primary key.
RequiresPrimaryKey=true
TableOnly=true
# Name the file like the class: E_DonateLeave -> E_DonateLeave.cs
OutputName={Table}.cs
